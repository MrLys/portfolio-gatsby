import React from "react";

const headerText = props => {
    return (
        <h1 style={{fontFamily: "Tangerine, serif",  fontSize: "60px", marginBottom: "0px", color: "#00adb5"}}> {props.headerText} </h1>
        );
}

export default headerText;
